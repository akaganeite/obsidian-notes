# 计划完成

- 支持fs相关系统调用
- fs unsafe问题

# 实际完成

- syscall：支持fstat fcntl getcwd symlink
- redoxfs：
  - 发现redoxfs的两个bug并解决，去掉alloclist链表，防止该链表占满磁盘空间
  - 将所有fs相关数据结构derive Pod并提供safe的强转方法
  - 代价：由于所有数据结构中的数据都需要是owned value，不能有Box<T>  出现，栈开销很大。
- debug：
  - 解决syspipe，sysopen，sysread实现时的逻辑问题
  - 优化fs相关系统调用代码实现逻辑，减少冗余代码
