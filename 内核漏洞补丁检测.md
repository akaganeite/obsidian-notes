# linux内核漏洞补丁存在性检测



## 项目结构

```
.
├── dataset
│   ├── binary
│   │   ├── check_condition_patch_ko.png
│   │   ├── check_condition_patch_mem.png
│   │   ├── check_condition_patch_vmlinux.png
│   │   ├── CVE_info.jsonl
│   │   ├── extract.py
│   │   ├── ko.py
│   │   ├── linux
│   │   ├── target
│   │   └── vuln_patch.ko
│   ├── cves.jsonl
│   ├── diff
│   │   ├── CVE-2023-12345_111111.diff
│   │   ├── CVE-2023-38409_fffb0b.diff
│   │   └── CVE-2023-52596_315552.diff
│   ├── sigs
│   │   ├── CVE-2023-12345.pkl
│   │   ├── CVE-2023-38409.pkl
│   │   ├── CVE-2023-52596.pkl
│   │   └── target.pkl
│   └── test.jsonl
├── debug
│   ├── CVE-2023-38409.sigs
│   ├── CVE-2023-52596.sigs
│   ├── target_sigs_check_conditions.log
│   ├── target_sigs_get_links.log
│   ├── target_sigs_insert_header.log
│   └── target_sigs_set_con2fb_map.log
├── ko
├── ps3
├── README.md
├── verifier
└── visualization
    ├── angr-utils
    └── bingraphvis
```

- dataset/binary
  - 三个图分别是从.ko,内存dump内容，vmlinux中提取出的cfg
  - ko.py与extract.py用于从bin文件或者vmlinux/ko文件中提取二进制内容，输出为elf
- sigs
  - 提取出的.pkl文件
- debug/
  - 不同函数以及不同cve生成的签名的log
- ko/
  - 当前测试使用的ko源码
- ps3/
  - 项目源码
- verifier/
  - 从kallsyms读取内存地址并dump至bnary.bin的工具
- visualization/
  - 用于angr可视化cfg

## CVES

- CVE-2023-38409，生成出的签名带有内存地址
- CVE-2023-52596，生成出的签名是OK的，可以使用
- CVE-2023-12345，我自己写的ko临时命名的CVE-id



# 检测流程(以CVE-2023-38409为例)

## 准备Linux内核二进制

1. CVE选取：CVE-2023-38409

   - https://www.cve.org/CVERecord?id=CVE-2023-38409
   - Patch: https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git/commit?id=fffb0b52d5258554c645c966c6cbef7de50b851d
   - Commit: fffb0b52d5258554c645c966c6cbef7de50b851d
   - 更改函数：`drivers/video/fbdev/core/fbcon.c:set_con2fb_map`
2. linux内核编译：

   - menuconfig选项，确保开启：
     - Device Drivers > Graphics support > Frame buffer Devices > Support for frame buffer devices
     - Kernel hacking > Compile-time checks and compiler options > Debug information > 只要不选disable debug information就可以
   - 内核版本信息：
     - Patched commit : __fffb0b52d5258554c645c966c6cbef7de50b851d__
     - Unpatched commit : __85e068 9eb6b10cd3b2fb455d1b3f4d4d0b13ff78__

## 运行ps3

### 准备patch/vuln与target binary

```bash
cd /path/to/ps3
mkdir -p dataset/binary/linux
cp path/to/patched_kernel dataset/binary/linux/CVE-year-id_commit_patch 
cp path/to/vuln_kernel dataset/binary/linux/CVE-year-id_commit_vuln
cp path/to/target_kernel dataset/binary/linux/LinuxKernel_cve_id
```

- commit是kernel打patch那个commit的前6位

### 准备diff文件

```
cp path/to/diff dataset/diff/CVE-year-id_commit.diff
```

 ### 修改json配置文件

- test.jsonl

  ```json
  {
   "file": "LinuxKernel_38409",//target_kernel文件名
   "cve": "CVE-2023-38409", //cve_id
   "commit": "fffb0b52d5258554c645c966c6cbef7de50b851d", //完整的patch commit id
   "ground_truth": "patch", //target是patch或vuln
   "project": "linux" //对应dataset/binary/linux目录
  }
  ```

- CVE_info.jsonl：无需内容，文件为空程序也可以正常运行

### 提取目标函数elf

 ```
 python dataset/binary/extract.py
 ```

- 将target-binary加载为angr project，将函数内存内容dump至`binary.bin`
- 将binary.bin封装为elf文件，添加目标函数名为symbol
- target-binary，提取的函数，以及输出路径均在CVE_info.jsonl中指定

### 运行

#### 离线签名生成

```
python ps3/main.py --gen
```

- 生成patch与vuln的binary的signature
- 存在generated_signature.pkl文件中

#### 在线签名匹配

```
#测试dataset/test.jsonl中指定的CVE
python ps3/main.py --test

#更便捷的测试
python ps3/verify.py

sigs=load_sigs_from_file("../dataset/sigs/CVE-2023-38409.pkl") # 指定sigs路径
testor=Test(sigs)
testor.test_path("../dataset/binary/target/CVE-2023-38409_fffb0b_patch.elf") #指定target binary路径
```



# 对ko文件的签名生成与匹配

## 准备patch和vuln版本的linux内核

- 编译好的版本：

  - CVE-2923-12345_111111_patch
  - CVE-2923-12345_111111_vuln

- 对内核源码做了什么更改：

  ```c
  //vuln,在fs/proc/proc_sysctl.c中增加如下代码
  struct custom_header {
      int size;
      int flag;
      int data;
  };
  
  // Prototype declarations
  bool is_flag_invalid(int flag);
  bool check_conditions(struct custom_header *header);
  
  
  bool is_flag_invalid(int flag) {
      // 返回条件：flag == 0 或其他复杂逻辑
      return (flag == 0 || (flag & 0xF) == 0);
  }
  
  
  
  bool check_conditions(struct custom_header *header) {
      // Memory access condition 1 (offset + 16)
      if (*(int *)((char *)header + 16) == 1) {
          return false;  // 第一个条件
      }
  
      return true;
  }
  //patch
  diff --git a/fs/proc/proc_sysctl.c b/fs/proc/proc_sysctl.c
  index 1c5a5ff8781f..8d62a3b27adf 100644
  --- a/fs/proc/proc_sysctl.c
  +++ b/fs/proc/proc_sysctl.c
  @@ -80,0 +81,3 @@ bool check_conditions(struct custom_header *header) {
  +       if (header->size == 0 || is_flag_invalid(header->flag)) {
  +               return true;  
  +       }
  ```

## 准备检测相关文件

- 在`dataset/binary/linux`下

  - CVE-2923-12345_111111_patch
  - CVE-2923-12345_111111_vuln

- `dataset/diff/CVE-2023-12345_111111.diff`

- test.jsonl

  ```json
  {"file": "CVE-2023-12345_111111_patch", "cve": "CVE-2023-12345", "commit": "1111118cdd9464949f8bdd3965bca49cf656eda6", "ground_truth": "patch", "project": "linux"}
  ```

## 编译ko

```
//在ko路径下
make
//复制ko文件到dataset/binary
cp vuln_patch.ko ../dataset/binary
//打ko
sudo insmod vuln_patch.ko
```

## 编译verifier

```bash
make
//运行python程序以dump内存内容至binary.bin
python kallsyms.py
```

## 准备target elf 文件

```
//在dataset/binary下，确保binary/有dump出的binary.bin和vuln_patch.ko
//更改ko.py的参数，针对bin和ko文件生成target elf文件
python ko.py
//在输出目标elf的同时也会将目标函数的cfg可视化
```

## 测试

```
python ps3/verify.py
```

- 在main函数中指定不同的参数以测试不同的target elf

## 现存问题

### CVE-2023-12345(`现存主要问题`)

- ps3无法直接对patch和vuln的ko文件生成签名，我把ko中的目标函数复制到linux源码中，编译。让ps3分析编译好的vmlinux，可以生成出正确的签名。
- 用该签名匹配ko文件中提取出的目标函数二进制，可以得到正确结果
- 匹配insmod ko文件后，从内存拿出来的二进制内容时，发现二进制内容无法通过符号执行得到签名。
  - debug/下以target开头的文件是target binary中每个函数生成的签名。
    - target_sigs_check_conditions.log是ko文件中提取出的签名，是正确的
    - 内存二进制提取出的签名，是空的，无法匹配。

# 





# CVE支持

## CVE-2023-38409

- linux编译选项如何配置已在上文中说明

### 不同commit测试结果

- vuln:85e068

| patch  |  vuln  | target | result: correct? |
| :----: | :----: | :----: | :--------------: |
| fffb0b | 85e068 | 85e068 |     vuln：✔      |
| 9b2ffa | 85e068 | 85e068 |     vuln：✔      |
| fffb0b | 85e068 | fffb0b |     patch: ✔     |
| 9b2ffa | 85e068 | 9b2ffa |     vuln: ❌      |

f9dc6b875ec0a6a6d4091cd9603d193ec98c75a2

## CVE-2023-52838

- [CVE-2023-52838 | CVE](https://www.cve.org/CVERecord?id=CVE-2023-52838)

- func：imsttfb_probe
- patch_commit: aba6ab57a910ad4b940c2024d15f2cdbf5b7f76b
  - 2023-11-07



## CVE-2024-26686

- [CVE-2024-26686 | CVE](https://www.cve.org/CVERecord?id=CVE-2024-26686)
- func：do_task_stat
- patch_commit:7601df8031fd67310af891897ef6cc0df4209305
  - 2024-01-23

## CVE-2023-52498

- [CVE-2023-52498 | CVE](https://www.cve.org/CVERecord?id=CVE-2023-52498)
- func:dev_pm_skip_resume
- patch_commit:7839d0078e0d5e6cc2fa0b0dfbee71de74f1e557 
  - 2024-01-03

## CVE-2023-52596

- [CVE-2023-52596 | CVE](https://www.cve.org/CVERecord?id=CVE-2023-52596)
- func:insert_header/get_links
- patch_commit:315552310c7de92baea4e570967066569937a843 
  - 2023-12-28

- diff:

  ```c
  diff --git a/fs/proc/proc_sysctl.c b/fs/proc/proc_sysctl.c
  index 8064ea76f80b50..84abf98340a049 100644
  --- a/fs/proc/proc_sysctl.c
  +++ b/fs/proc/proc_sysctl.c
  @@ -44,7 +44,7 @@ static struct ctl_table sysctl_mount_point[] = {
    */
   struct ctl_table_header *register_sysctl_mount_point(const char *path)
   {
  -	return register_sysctl_sz(path, sysctl_mount_point, 0);
  +	return register_sysctl(path, sysctl_mount_point);
   }
   EXPORT_SYMBOL(register_sysctl_mount_point);
   
  @@ -233,7 +233,8 @@ static int insert_header(struct ctl_dir *dir, struct ctl_table_header *header)
   		return -EROFS;
   
   	/* Am I creating a permanently empty directory? */
  -	if (sysctl_is_perm_empty_ctl_table(header->ctl_table)) {
  +	if (header->ctl_table_size > 0 &&
  +	    sysctl_is_perm_empty_ctl_table(header->ctl_table)) {
   		if (!RB_EMPTY_ROOT(&dir->root))
   			return -EINVAL;
   		sysctl_set_perm_empty_ctl_header(dir_h);
  @@ -1213,6 +1214,10 @@ static bool get_links(struct ctl_dir *dir,
   	struct ctl_table_header *tmp_head;
   	struct ctl_table *entry, *link;
   
  +	if (header->ctl_table_size == 0 ||
  +	    sysctl_is_perm_empty_ctl_table(header->ctl_table))
  +		return true;
  +
   	/* Are there links available for every entry in table? */
   	list_for_each_table_entry(entry, header) {
   		const char *procname = entry->procname;
  ```

  
