# SP&BP

SP&BP架构基于的安全假设是：CPU芯片是可信的，而所有其他硬件（如存储芯片、外部总线、硬盘驱动器等）都是不可信的。此外，架构假定只有虚拟机管理程序（Hypervisor）和小型可信软件模块是可信的，所有其他软件（如操作系统和应用程序）都是不可信的。

SP&BP架构要求对Hypervisor进行保护，并激活Hypervisor的安全存储功能，其中涉及的secure_launch指令需要计算Hypervisor的哈希值，并在不一致时锁定其安全存储区，这需要在处理器芯片上实现新的指令和机制。

在不改变现有Intel处理器权限模型的条件下，需要进行的改动和扩展：

1. **处理器微架构变更**：
   - **安全执行环境**：可能需要在处理器的微架构中添加一个安全执行环境，如SP（Secret Protected）区域，用于执行敏感操作，保证关键代码和数据的隔离。
   - **特殊的寄存器**：需要引入新的寄存器来维护和管理安全状态信息，例如模块身份、安全存储哈希值等。

2. **新的指令集添加**：
   
   - **安全启动指令**：比如secure_launch指令，用于初始化安全执行环境，验证Hypervisor的完整性，并激活安全存储功能。
   - **密钥管理指令**：用于生成、管理和使用加密密钥，以确保数据在传输和存储时的安全性。
   
3. **安全密钥和加密引擎集成**：
   
   - **内置TPM功能**：将TPM的功能集成到处理器中，用于安全启动和运行时完整性检查，以及安全存储密钥和其他敏感数据。
   - **加密/哈希引擎**：集成加密和哈希引擎，用于加密内存和存储，保证数据在内存中的安全性，并提供快速的哈希计算以支持各种安全协议。
   
4. **内存保护机制**：
   
   - **执行流控制**：可能需要增强处理器对执行流的控制，以确保只有经过验证的代码才能执行。
   - **内存访问控制**：需要改进内存访问控制机制，保证只有授权的代码才能访问指定的内存区域。
   
5. **硬件支持的虚拟化技术**：
   
   - **改善虚拟化支持**：需要强化处理器对虚拟化技术的支持，以实现对虚拟机和Hypervisor的细粒度安全控制。
   
     1. **安全分区**：在虚拟化环境中为每个虚拟机或容器提供独立的安全分区，这样即使是在同一个物理主机上，一个虚拟机的崩溃或安全漏洞也不会影响其他虚拟机。
   
     1. **细粒度访问控制**：对虚拟机和它们的Hypervisor之间进行更细粒度的访问控制，例如，控制它们访问硬件资源的能力。
   
     1. **安全加密支持**：加强处理器对虚拟机内存加密的支持，防止虚拟机的内存被未授权访问，即使是Hypervisor或物理攻击者。
   
     1. **可信度量和完整性检查**：增强虚拟化平台的可信启动和运行时完整性检查，确保只有被认证和信任的代码和数据可以在虚拟机中运行。
   
     1. **I/O设备和网络虚拟化**：提供更安全的I/O设备虚拟化，确保虚拟机对网络和存储的访问是安全和受控的。

# CHERI

CHERI（Capability Hardware Enhanced RISC Instructions）架构通过向传统的硬件指令集架构（ISA）添加能力（Capability）来提供细粒度的内存保护，并保护那些内存不安全的编程语言，例如C和C++。CHERI能力被设计为在CPU和SoC的微架构中实现，以允许软件展示出新的安全行为。

在不改变英特尔处理器权限模型的条件下实现CHERI是不可行的，因为它的运作方式与英特尔现有的保护机制根本不同。英特尔目前的模型并不原生支持能力（Capability）；它使用基于段和分页的模型来进行内存保护。要实现CHERI，需要：

1. 为能力引入一个新的数据类型，其宽度是常规整数指针的两倍。
2. 修改现有的ISA，包括针对能力的新安全原语。
3. 改进微架构，以在所有内存访问中执行能力检查。
4. 可能需要改变操作系统和编译器的行为，以创建和管理能力。

# Security-First

实现Security-First安全体系结构的关键在于构建一个主动安全处理器（ASP），它需要具有超出传统CPU权限的控制能力以及对系统的全面监控能力。在Intel现有的处理器权限模型下，处理器的特权级是固定的，且没有设计来支持一个完全独立的、具有最高访问权限的ASP。以下是为什么在不改变Intel处理器权限模型的条件下，实现Security-First安全体系结构可能不可行的原因：

1. **物理隔离需求**：Security-First体系结构要求计算处理器和安全处理器之间有物理隔离，并且具有单向的信息流。现有的Intel处理器架构并未设计来支持这样的硬件级别的分离和信息流控制。

2. **主动安全监控**：ASP的设计要能主动监测和控制计算子系统，这通常需要对硬件事件和计数器有更深入的访问权限，而这些功能在Intel的当前模型中通常是受限的，只能由操作系统或在特定权限模式下的软件访问。

3. **高权限操作**：在Security-First体系结构中，ASP需要具有"SuperRoot"的权限级别，这意味着它需要能够访问和控制所有资源，包括CPU、内存、I/O设备等。在Intel的处理器权限模型中，并没有定义比操作系统核心更高的权限级别。

为了设计和实现这样的安全体系结构，需要：

- 引入新的硬件模块和修改微架构，来支持物理隔离和ASP的主动监控。
- 在CPU中集成一个或多个安全协处理器，并为它们提供独立的资源和访问通道。
- 设计一种新的权限模型，允许ASP覆盖和控制现有的所有权限设置，且不能被任何操作系统或软件层级的组件所干预。

因此，在不改变Intel现有处理器权限模型的情况下，实现Security-First体系结构不太可能。如果想在现有架构上实现类似的安全功能，可能需要通过软件层面的严格隔离机制和增强的硬件支持，如安全启动、TPM等技术来间接实现，但这仍然无法完全等同于Security-First体系结构所提出的安全级别。