





<img src="https://th.bing.com/th?id=OIP.RW6DrG-Ydwz8wkXL_iolgAHaG4&w=259&h=241&c=8&rs=1&qlt=90&o=6&dpr=2&pid=3.1&rm=2" alt="img" style="zoom: 80%;" />





<h1 align='center'>
跨模态智能计算与应用
</h1>




<h1 align='center'>Text Is MASS: Modeling as Stochastic Embedding for Text-Video Retrieval
</h1>


















<h4 align='center'>姓名：张小北</h4>

<h4 align='center'>学号：202328018670109</h4>



### 1.研究背景

​	随着 YouTube、抖音、快手等视频媒体软件的出现和流行，越来越多的用户通过视频记录并分享日常生活，因此每天都有海量的视频、文本等数据在网络上产生和传播。如此纷繁复杂的数据导致传统的单模态检索限制了用户对获取信息全面性、灵活性的需求，也增加了企业对数据进行管理的成本。为了更好地解决这些问题，视频文本跨模态检索（video-text cross-modalretrieval, VTR）开始受到研究者的广泛关注。

### 2.问题描述

​	VTR任务旨在从海量无标注视频、文本中利用多模态数据的统一表达，分析不同模态数据的相关性，去除模态之间的冗余信息，实现视频和文本的相互检索，从而降低企业对数据进行管理的成本，并帮助人们从单模态检索限制的困境中解脱出来然而，由于视频数据编码的复杂性，不同模态数据底层特征之间的异构性，准确度量视频、文本之间的相似度，实现跨模态检索是一项困难和极具挑战的任务。

### 3.动机目标

​	最近的研究进展集中在建立一个文本和视频的联合嵌入空间，依靠一致的嵌入表示来计算相似性。然而，现有数据集中的文本内容通常是简短和简洁的，这使得很难完全描述视频的冗余语义。相应地，单个文本嵌入可能无法捕获视频嵌入并增强检索能力。该文章提出了一种新的随机文本建模方法T-MASS，即将文本建模为随机嵌入，以灵活和弹性的语义范围丰富文本嵌入。

### 4.基本原理

一般情况下，VTR任务可以分为视频特征提取、文本特征提取、特征嵌入与匹配以及目标函数四个部分。

- 视频特征提取(Video representation extraction)

  视频特征提取模块用于捕捉视频特征表示，根据时空特性可以将视频特征提取器分为空间特征提取器和时间特征提取器。此外，由于视频中涉及多模态信息（例如，运动、音频或面部特征），通常使用额外的专家来提取每个模态的特征，然后聚合以生成更全面的视频表示。

- 文本特征提取(Textual representation extraction)

  文本特征提取模块用来提取文本特征，提取器主要建立在RNN、CNNs和Transformer上。

- 特征嵌入与匹配(Feature embedding and matching)

  特征嵌入与匹配模块用来映射和对齐提取得到的视频和文本特征，并计算它们的相似性得分（也称为兼容性）。其核心是如何将这两种模式嵌入到一个联合的特征空间中，并判断它们是否匹配。为了弥合语义差距，更准确地度量相似性，典型的方法可以根据语义层次将语义划分为全局、局部和个体匹配。此外，大多数工作考虑全局和局部特征的结合。

- 目标函数(Objectives functions)

  目标函数是为了训练和细化来自提取器的特征表示和来自特征匹配的相似性分数。现有的方法定义了多种目标函数，大致可以分为三重态损失、对比损失和其他损失。

### 1.研究背景

随着 YouTube、抖音和快手等视频媒体平台的兴起，越来越多的用户通过视频记录和分享他们的日常生活。这导致每天在网络上传播和生成大量的视频和文本数据。这些数据的复杂性和多样性使得传统的单模态检索无法满足用户对信息获取的全面性和灵活性的需求，同时也增加了企业管理数据的成本。为了解决这些问题，视频文本跨模态检索（video-text cross-modal retrieval, VTR）开始受到广泛关注。

### 2.问题描述

VTR 任务的目标是利用多模态数据的统一表达，从海量无标注的视频和文本中分析不同模态数据之间的相关性，去除冗余信息，实现视频和文本的相互检索。这样不仅可以降低企业的数据管理成本，还能克服单模态检索的限制。然而，由于视频数据编码的复杂性和不同模态数据底层特征之间的差异，准确度量视频和文本之间的相似度，实现跨模态检索是一项复杂且具有挑战性的任务。

### 3.动机与目标

最近的研究主要集中在建立一个文本和视频的联合嵌入空间，利用一致的嵌入表示来计算相似性。然而，现有数据集中的文本内容通常简短且简洁，难以完全描述视频的丰富语义。因此，单个文本嵌入可能无法充分捕捉视频的嵌入，从而影响检索能力。为了解决这个问题，提出了一种新的随机文本建模方法 T-MASS，将文本建模为随机嵌入，以丰富文本嵌入的语义范围，从而增强检索能力。

### 基本原理

VTR 任务通常分为视频特征提取、文本特征提取、特征嵌入与匹配以及目标函数四个部分。

- **视频特征提取 (Video representation extraction)**

  视频特征提取模块用于捕捉视频特征表示。根据时空特性，可以将视频特征提取器分为空间特征提取器和时间特征提取器。此外，由于视频中包含多模态信息（例如，运动、音频或面部特征），通常使用额外的专家来提取每个模态的特征，然后将这些特征聚合生成更全面的视频表示。

- **文本特征提取 (Textual representation extraction)**

  文本特征提取模块用于提取文本特征，主要基于 RNN、CNNs 和 Transformer 架构。

- **特征嵌入与匹配 (Feature embedding and matching)**

  特征嵌入与匹配模块用于映射和对齐提取到的视频和文本特征，并计算它们的相似性得分（也称为兼容性）。核心问题在于如何将这两种模式嵌入到一个联合的特征空间中，并判断它们是否匹配。为了更准确地度量相似性，典型的方法根据语义层次将语义划分为全局、局部和个体匹配，大多数工作都考虑了全局和局部特征的结合。

- **目标函数 (Objectives functions)**

  目标函数用于训练和细化来自提取器的特征表示以及来自特征匹配的相似性分数。现有方法定义了多种目标函数，主要包括三重态损失、对比损失和其他损失。

### 5.核心算法

### 方法总览

![image-20240628221530155](C:\Users\jetta\AppData\Roaming\Typora\typora-user-images\image-20240628221530155.png)图中展示了CLIP-ViP框架的核心组成部分，包括文本编码器、视频编码器、代理引导的注意力机制和全源跨模态学习方法。

1. 文字编码器 (Text Encoder)

   - **输入**：文本编码器处理字幕和图像字幕，将其转换为文本token。

   - **处理**：每个文本token都会有对应的位置嵌入，用于后续的跨模态对齐。

2. 视频编码器 (Vision Encoder with Proxy-guided Attention)

   - **输入**：视频编码器接收视频帧和视频代理token。

   - **处理**：通过代理引导的注意力机制，视频编码器能够在处理视频帧时有效感知时间信息。这种设置允许视频编码器在处理图像和视频时保持一致的结构。

3. 全源跨模态学习 (Omnisource Cross-modal Learning in a Batch)
   - **学习过程**：图中展示了四种不同的跨模态损失计算方法，用于在批处理中的全源跨模态学习。这些方法结合了视频-字幕、帧-字幕和图像字幕的对齐损失，以提高跨模态表示的准确性。

4. 注意力机制
   - **注意力掩码**：通过代理引导的注意力机制，视频代理token关注所有补丁token，而每个补丁token只关注同一帧内的补丁和视频代理token。这种机制帮助模型更好地整合视频的时空信息。

#### 域内辅助数据生成
为了弥合语言领域的差距，研究人员在大规模视频-字幕数据中引入了辅助字幕，而不是使用可能导致数据泄漏的视频字幕生成模型。他们采用图像字幕生成模型（OFA-Caption）为HD-VILA-100M数据集的每个视频的中间帧生成字幕。这种方法生成了1亿条长度最多为16个单词的句子，有助于减少预训练和下游数据之间的语言领域差距。

#### 视频代理机制
该论文提出了一种新颖的视频代理机制，使Vision Transformer（ViT）模型能够同时处理图像和视频编码。视频代理token作为代理，帮助每个局部补丁感知视频级的时间信息。修改后的注意力机制使视频代理token能够关注所有token，而补丁token只关注同一帧内的token和视频代理token。这种设置允许视觉编码器以最小的修改处理图像和视频输入，保留了ViT骨干的通用性和扩展性。视频代理机制的公式如下：

$$
g(ft_{n}) = Linear(ft_{n}) + P_{oss}(n) + P_{ost}(t)
$$
其中，$Linear(*)$ 是一个线性层，$P_{oss}(n)$ 和 $P_{ost}(t)$ 分别是可学习的空间和时间位置嵌入。整个视频可以被划分为 $T \times N$ 个补丁token。通过这种方法，CLIP-ViP模型在学习视频-语言表示方面表现出色，能够很好地泛化到不同的数据集和任务上。

#### 全源跨模态学习（OCL）
为了从视频-字幕对中学习丰富的视频-语言对齐，并减少辅助帧-字幕对的语言领域差距，作者提出了全源跨模态学习（OCL）方法。该方法通过info-NCE损失进行对比学习，从视频-文本和图像-文本对中联合学习跨模态表示。OCL方法包括各种损失组合，以探索更好的表示学习策略，例如结合视频-字幕对和帧-字幕对。在对比学习中使用了以下损失函数：
$$
L_{v2t} = -\frac{1}{B} \sum_{i=1}^{B} \log \frac{e^{v_i^\top t_i / \tau}}{\sum_{j=1}^{B} e^{v_i^\top t_j / \tau}}\\
 L_{t2v} = -\frac{1}{B} \sum_{i=1}^{B} \log \frac{e^{t_i^\top v_i / \tau}}{\sum_{j=1}^{B} e^{t_i^\top v_j / \tau}}
$$
其中，$v_i$ 和 $t_i$ 分别表示第 $i$ 个视觉特征和文本特征的归一化嵌入，$\tau$ 是可学习的温度参数。总体对齐损失 $L_{X \leftrightarrow Y}$ 是 $L_{v2t}$ 和 $L_{t2v}$ 的平均值。例如，$L_{V \leftrightarrow S}$ 表示在视频-字幕对中的 info-NCE 损失。

### 6.实验方法

1. **视频-文本后预训练**
   - **数据采样**：从平均长度为13.4秒的视频片段中均匀采样12帧，并将所有帧调整为224×224的尺寸。文本部分使用CLIP的tokenizer将句子分割为长度最多为70的词令牌。
   - **优化器和训练参数**：使用AdamW优化器，初始学习率设为5e-6，权重衰减固定为5e-2。采用余弦衰减的学习率调度策略，并配合热身策略。模型在32个NVIDIA Tesla V100 GPU上进行训练，批次大小为1024。对比相似度在所有GPU上聚合的特征上计算。所有消融研究的训练步骤设为在HD-VILA-100M上进行一个epoch，完整设置则进行三个epoch。

2. **微调训练**
   - **参数调整**：在微调过程中，重用了大部分后预训练的超参数，除了以下几点：
     1. 批次大小：为所有下游任务设置批次大小为128，以进行公平比较。
     2. 学习率和权重衰减：分别设为1e-6和0.2。
     3. 训练轮数：根据下游数据集的规模，设置MSR-VTT、DiDeMo、LSMDC和ActivityNet的数据集的训练轮数分别为5、20、10和20。
     4. 帧数：为公平比较，设置帧数为12，除了ActivityNet Captions数据集（设置为32），因为该数据集的视频长度较长（平均180秒）。

### 7.结果分析

#### 实验结果

1. **post-pretrain数据消融研究**
   - 结果表明，在MSR-VTT和DiDeMo数据集上的文本到视频检索结果如下：

     | Post-pretrain Data        | MSR-VTT Retrieval R@1 | R@5  | R@10 | Mean | DiDeMo Retrieval R@1 | R@5  | R@10 | Mean |
     | ------------------------- | --------------------- | ---- | ---- | ---- | -------------------- | ---- | ---- | ---- |
     | w/o Post-pretrain         | 46.5                  | 72.1 | 82.5 | 67.0 | 40.6                 | 70.4 | 79.3 | 63.4 |
     | HD-VILAsub                | 47.7                  | 72.1 | 82.4 | 67.4 | 44.6                 | 73.9 | 81.9 | 66.8 |
     | HD-VILAcap                | 45.9                  | 73.0 | 81.8 | 66.9 | 44.9                 | 74.4 | 82.3 | 67.2 |
     | HD-VILAsub+cap            | 49.6                  | 74.5 | 84.8 | 69.6 | 48.2                 | 76.7 | 84.4 | 69.8 |
     | ImageCaption              | 45.6                  | 70.7 | 81.1 | 65.8 | 43.7                 | 69.5 | 77.9 | 63.7 |
     | HD-VILAsub + ImageCaption | 49.1                  | 73.1 | 83.5 | 68.6 | 47.0                 | 75.3 | 84.1 | 68.8 |

2. **不同模型结构的微调结果**
   - 在MSR-VTT文本到视频检索任务上的结果如下：

     | Model                | R@1  | R@5  | R@10 | Mean |
     | -------------------- | ---- | ---- | ---- | ---- |
     | MeanPool             | 43.4 | 70.9 | 81.1 | 65.1 |
     | SeqTransformer       | 44.6 | 71.2 | 81.8 | 65.9 |
     | Full Attention       | 42.8 | 70.1 | 80.3 | 64.4 |
     | 2 Video Proxy Tokens | 45.8 | 71.3 | 81.7 | 66.3 |
     | 4 Video Proxy Tokens | 46.5 | 72.1 | 82.5 | 67.0 |
     | 8 Video Proxy Tokens | 45.7 | 72.7 | 81.7 | 66.7 |

3. **对更多下游任务的评估**
   - 在HMDB51、SSv2、MSR-VTT MC和LSMDC MC数据集上的零样本准确率（%）如下：

     | Model                      | HMDB51 | SSv2 | MSR-VTT MC | LSMDC MC |
     | -------------------------- | ------ | ---- | ---------- | -------- |
     | CLIP-MeanPool              | 40.3   | 25.8 | 89.9       | 64.3     |
     | CLIP-ViP w/o post-pretrain | 41.1   | 26.1 | 89.4       | 63.5     |
     | CLIP-ViP w/ video-subtitle | 36.6   | 28.0 | 86.6       | 67.0     |
     | CLIP-ViP full              | 44.8   | 33.0 | 90.3       | 67.6     |



从实验结果可以看出，CLIP-ViP模型在文本到视频检索任务上的表现大大优于基准模型。同时，消融研究验证了每个部分的有效性，特别是视频代理机制和全源跨模态学习（OCL）方法。该方法在多个广泛使用的基准测试中取得了最新的最佳结果，表明CLIP-ViP在学习视频-语言表示方面的优越性和良好的泛化能力。

### 8.总结展望

### 结论

- 本文提出的CLIP-ViP框架通过引入域内辅助数据生成、视频代理机制和全源跨模态学习（OCL）方法，为视频-文本跨模态检索任务提供了一个有效的解决方案。
- 在域内辅助数据生成方面，通过采用图像字幕生成模型（OFA-Caption），为每个视频的中间帧生成辅助字幕，成功减少了预训练和下游数据之间的语言领域差距。这样的方法不仅增强了文本的表达能力，还避免了数据泄漏的问题。
- 视频代理机制的引入是该框架的一个重要创新。保留了ViT骨干的通用性和扩展性，有效地提高了视频编码的效率和质量。
- 全源跨模态学习（OCL）方法通过info-NCE损失进行对比学习，从视频-文本和图像-文本对中联合学习跨模态表示。该方法包括多种损失组合，以探索更好的表示学习策略，从而提升跨模态检索的准确性。
- 本文提出的CLIP-ViP框架通过创新性的技术和方法，有效解决了视频-文本跨模态检索中的多个难题。通过域内辅助数据生成、视频代理机制和全源跨模态学习（OCL）方法的结合，CLIP-ViP展示了其在学习视频-语言表示方面的优越性和良好的泛化能力，为未来的跨模态检索研究提供了有力的支持和参考。

